# Web Launcher Dependencies
# Minimal requirements for the launcher web server

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
jinja2>=3.1.2
python-multipart>=0.0.6
aiohttp>=3.9.0

# Note: launcher_lib dependencies (psutil, packaging, requests)
# are inherited from the main requirements_server.txt
